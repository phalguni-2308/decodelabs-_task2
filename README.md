# decodelabs-_task2
Backend API internship project: Express.js server with /items GET (list all) and POST (add new) endpoints. Includes full request validation (non‑empty name, positive price) and in‑memory storage.
